# Render.com configuration
# Make sure to set Root Directory to: backend
# This file ensures only backend is deployed

services:
  - type: web
    name: omegoo-backend4
    env: node
    region: oregon
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    rootDir: backend