# Production Environment Variables for Render

## Copy these to Render Dashboard → Environment Variables

### Core Configuration
NODE_ENV=production
PORT=10000
USE_MONGODB=true

### MongoDB (Your existing connection)
MONGODB_URI=mongodb+srv://omegoo_db_user:omegoo_pass@cluster0.fabck1e.mongodb.net/omegoo_db?retryWrites=true&w=majority

### Frontend URL (Vercel deployment)
FRONTEND_URL=https://omegoo.vercel.app
ALLOWED_ORIGINS=https://omegoo.vercel.app

### JWT Secrets (PRODUCTION - MUST CHANGE)
# Generate using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=7a8f3b2e9d4c1a6f8e5b3d7c9a2f4e1b8d6c3a9f7e4b2d8c5a1f3e6b9d4c7a2f
REFRESH_TOKEN_SECRET=9b4d8f2a6e3c7b5d9a1f8e4c6b2d7a5f3e9c1b8d4a7f2e6c9b3d5a8f1e4c7b2d
ADMIN_JWT_SECRET=3e7b9d4f2a8c6e1b5d9a7f3e8c2b6d4a9f1e7c5b3d8a6f4e2c9b1d7a5f8e3c4b

### JWT Expiry
JWT_EXPIRE=24h
REFRESH_TOKEN_EXPIRE=7d
ADMIN_JWT_EXPIRE=12h

### Security
BCRYPT_ROUNDS=10
SESSION_SECRET=8c5f3b9e2d7a4f1c6e9b3d8a5f2c7e4b1d9a6f3e8c5b2d7a4f9e1c6b8d3a5f7e

### Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

### Optional (if using Twilio for OTP)
# TWILIO_ACCOUNT_SID=your_actual_twilio_sid
# TWILIO_AUTH_TOKEN=your_actual_twilio_token
# TWILIO_PHONE_NUMBER=+1234567890
OTP_EXPIRE_MINUTES=5

### Logging
LOG_LEVEL=info

### Redis (Optional - using in-memory for now)
REDIS_URL=memory://localhost

---

## How to Add in Render:

1. Go to https://dashboard.render.com
2. Select your backend service (omegoo-backend)
3. Click "Environment" in left sidebar
4. For each variable above:
   - Click "Add Environment Variable"
   - Copy Key and Value
   - Click "Save"
5. After adding all variables, click "Save Changes"
6. Service will automatically redeploy

## Security Notes:

⚠️ **IMPORTANT**: The JWT secrets above are randomly generated for production.
They are different from your development secrets for security.

✅ **Safe to use**: These are production-grade random secrets
🔒 **Never commit**: Keep these in Render environment variables only
📝 **Save backup**: Keep a copy of these secrets in a secure password manager

## Testing After Deployment:

1. Visit: https://your-backend.onrender.com/api/health
2. Should return: {"status":"healthy","mongodb":"connected"}
3. Try login from frontend: https://omegoo.vercel.app
